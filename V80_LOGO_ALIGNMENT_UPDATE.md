# V80 — Logo Alignment

- Preserved the fixed navbar and investigation drawer fixes from V79.
- Adjusted only the ProcureIQ brand visual anchor so its left edge aligns with the sidebar account-name anchor.
- Workspace context and right-side controls retain their layout footprint.
- Mobile layout resets the logo transform.
- Workspace CSS cache/build version bumped to V80.
